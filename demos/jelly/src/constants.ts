import { d } from 'typegpu';

export const MAX_STEPS = 100;
export const MIN_DIST = 0.001;
export const MAX_DIST = 100.0;
export const BACKGROUND_COLOR = d.vec3f(0.1, 0.1, 0.1);
export const SLIDER_COLOR = d.vec3f(1.0, 0.45, 0.07);
export const GROUND_COLOR = d.vec3f(1.0, 1.0, 1.0);
export const TEXT_COLOR = d.vec3f(0.15, 0.15, 0.15);
